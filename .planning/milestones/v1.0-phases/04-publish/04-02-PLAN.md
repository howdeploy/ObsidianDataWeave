---
phase: 04-publish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
  - templates/Notes/Atomic Note Example.md
  - templates/MOCs/Topic Map - MOC.md
  - templates/Sources/.gitkeep
  - templates/.smart-env/smart_env.json
  - templates/README.md
autonomous: true
requirements:
  - DIST-01
  - DIST-03
  - DIST-04
must_haves:
  truths:
    - "README contains 2-3 copy-paste commands a user sends to Claude for installation and first document processing"
    - "README has bilingual EN+RU sections in a single file"
    - "README has a brief MOC + Zettelkasten introduction (1-2 paragraphs)"
    - "templates/ directory contains an example atomic note with correct v1 frontmatter schema"
    - "templates/ directory contains an example MOC with correct v1 frontmatter schema and wikilinks"
    - "templates/.smart-env/smart_env.json exists with recommended Smart Connections config"
    - "Copying templates/ to a vault root creates a working starter Obsidian structure"
  artifacts:
    - path: "README.md"
      provides: "Bilingual EN+RU documentation with Quick Start commands"
      min_lines: 80
    - path: "templates/Notes/Atomic Note Example.md"
      provides: "Example atomic note with v1 frontmatter"
      contains: "note_type: atomic"
    - path: "templates/MOCs/Topic Map - MOC.md"
      provides: "Example MOC with v1 frontmatter and wikilinks"
      contains: "note_type: moc"
    - path: "templates/.smart-env/smart_env.json"
      provides: "Ready-to-use Smart Connections configuration"
      contains: "bge-micro-v2"
    - path: "templates/README.md"
      provides: "Template usage instructions"
      min_lines: 10
  key_links:
    - from: "README.md"
      to: "install.sh"
      via: "Quick Start step 1 references bash install.sh"
      pattern: "bash install\\.sh"
    - from: "README.md"
      to: "scripts/process.py"
      via: "Quick Start step 3 references python3 scripts/process.py"
      pattern: "python3 scripts/process\\.py"
    - from: "templates/Notes/Atomic Note Example.md"
      to: "templates/MOCs/Topic Map - MOC.md"
      via: "Wikilink from atomic note to concept referenced in MOC"
      pattern: "\\[\\[.*\\]\\]"
---

<objective>
Create the bilingual README.md with zero-to-first-note Quick Start, Obsidian vault templates (example notes, folder structure), and Smart Connections config. After this plan, a new user reading the README can copy-paste commands into Claude Code and process their first document.

Purpose: DIST-01 (copy-paste commands for Claude), DIST-03 (Obsidian templates), DIST-04 (Smart Connections config). This is the user-facing documentation and starter kit.
Output: README.md, templates/ directory with example notes and Smart Connections config
</objective>

<execution_context>
@/home/kosya/.claude/get-shit-done/workflows/execute-plan.md
@/home/kosya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-publish/04-RESEARCH.md
@.planning/phases/04-publish/04-CONTEXT.md
@.planning/phases/04-publish/04-01-SUMMARY.md
@config.example.toml
@tags.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Obsidian vault templates</name>
  <files>
    templates/Notes/Atomic Note Example.md
    templates/MOCs/Topic Map - MOC.md
    templates/Sources/.gitkeep
    templates/.smart-env/smart_env.json
    templates/README.md
  </files>
  <action>
Create the templates/ directory structure with all example files.

1. **templates/Notes/Atomic Note Example.md**: English-language example atomic note demonstrating correct v1 frontmatter schema. Use these exact frontmatter fields (schema v1 locked):
```yaml
---
tags:
  - productivity/zettelkasten
  - productivity/pkm
date: 2026-02-26
source_doc: "Example Document.docx"
note_type: atomic
---
```
Body: 150-300 words explaining the atomic note principle — one idea per note, why it matters for search and linking. Include 1-2 inline wikilinks (e.g., `[[Semantic Search with Smart Connections]]`, `[[MOC as Navigation Hub]]`) to demonstrate proper wikilink format.

2. **templates/MOCs/Topic Map - MOC.md**: English-language example MOC demonstrating the two-level hierarchy structure. Frontmatter:
```yaml
---
tags:
  - productivity/moc
  - productivity/pkm
date: 2026-02-26
source_doc: "Example Document.docx"
note_type: moc
---
```
Body: Two H2 sections ("Core Concepts", "Methodology"), each with 2-3 wikilinked bullet items. One of the links should reference "Atomic Note Example" to show cross-referencing. Use `[[Wikilink Title]]` format (not markdown links).

3. **templates/Sources/.gitkeep**: Empty file to preserve the Sources/ directory in git.

4. **templates/.smart-env/smart_env.json**: Ready-to-use Smart Connections configuration. Use the exact JSON from the research findings:
```json
{
  "is_obsidian_vault": true,
  "smart_blocks": {
    "embed_blocks": true,
    "min_chars": 100
  },
  "smart_sources": {
    "min_chars": 100,
    "embed_model": {
      "adapter": "transformers",
      "transformers": {
        "model_key": "TaylorAI/bge-micro-v2"
      }
    },
    "excluded_headings": "",
    "file_exclusions": "Untitled",
    "folder_exclusions": ""
  },
  "language": "en",
  "connections_lists": {
    "results_collection_key": "smart_sources",
    "score_algo_key": "similarity",
    "connections_post_process": "none",
    "results_limit": 20,
    "exclude_frontmatter_blocks": true
  }
}
```
Key choices: TaylorAI/bge-micro-v2 (free, local, no API key), min_chars 100 (captures all atomic notes), embed_blocks true (paragraph-level connections).

5. **templates/README.md**: Short instructions (10-20 lines) explaining how to use templates — copy the folder contents to your Obsidian vault root, delete example files after understanding the format, copy .smart-env/ to vault root for Smart Connections.
  </action>
  <verify>
Verify directory structure: `find templates/ -type f | sort`. Verify frontmatter in atomic note: `head -8 "templates/Notes/Atomic Note Example.md"` shows correct v1 schema. Verify MOC has wikilinks: `grep -c "\[\[" "templates/MOCs/Topic Map - MOC.md"` returns 4+. Verify smart_env.json is valid JSON: `python3 -m json.tool "templates/.smart-env/smart_env.json"`.
  </verify>
  <done>templates/ directory contains: 1 atomic note with v1 frontmatter and wikilinks, 1 MOC with two-level hierarchy and wikilinks, Sources/.gitkeep, .smart-env/smart_env.json with recommended config, and a short README.md with usage instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Bilingual README.md with Quick Start</name>
  <files>README.md</files>
  <action>
Create README.md as a bilingual EN+RU document. Structure (per user decisions):

**English section first**, then **Russian section** (separated by a horizontal rule and language header).

### English Section:

1. **Header + badge area**: `# ObsidianDataWeave` with a one-line description: "Turn research documents from Google Drive into structured, linked atomic notes in Obsidian."

2. **What is this?** (2-3 sentences): Claude Code skill that fetches .docx from Google Drive, splits into atomic notes (MOC + Zettelkasten methodology), assigns tags and wikilinks, writes to your Obsidian vault.

3. **MOC + Zettelkasten intro** (1-2 paragraphs per user decision): Brief explanation of the methodology — Maps of Content as navigation hubs, atomic notes as single-idea units, wikilinks for semantic connections, Smart Connections for vector search. Keep it practical, not academic.

4. **Quick Start** (THE most important section — DIST-01):
   Format as numbered steps that a user copies directly into Claude Code:

   ```
   ## Quick Start

   Open Claude Code in your terminal and send these commands:

   **1. Install**
   > Clone the repo and run the installer:
   > `git clone https://github.com/USER/ObsidianDataWeave.git && cd ObsidianDataWeave && bash install.sh`

   **2. Configure rclone** (if not done yet)
   > Set up Google Drive access:
   > `rclone config`
   > Create a remote named `gdrive:` pointing to your Google Drive.

   **3. Process your first document**
   > `process MyResearch.docx`
   ```

   Use `USER` as GitHub username placeholder. Keep commands minimal — Claude handles the rest per the registered skill.

5. **What happens**: Brief pipeline explanation (fetch -> parse -> atomize -> generate -> write to vault). One sentence per step.

6. **Templates**: Explain the templates/ directory — copy to vault root for starter structure, delete examples after understanding format. Mention .smart-env/ for Smart Connections.

7. **Configuration**: Reference config.example.toml, explain key fields briefly (vault_path, notes_folder, moc_folder, source_folder, rclone remote).

8. **Requirements**: Python 3.10+ (3.11+ recommended), rclone, Claude Code, Obsidian with Smart Connections plugin (optional).

9. **License**: MIT — see LICENSE file.

### Russian Section:

After `---` separator and `# ObsidianDataWeave (RU)` header, provide the same content in Russian. Same structure: brief description, Quick Start with the same commands (commands stay in English — they're code), pipeline explanation, templates, configuration.

Key constraints:
- Quick Start commands MUST be optimized for copy-paste into Claude Code (per user decision)
- Minimum explanations — user copies commands, Claude handles the rest (per user decision)
- Do NOT include git push instructions (deferred — user tests locally first)
- Commands should use relative paths within the repo (user is cd'd into the repo from step 1)
  </action>
  <verify>
Verify README structure: `grep -c "^#" README.md` shows multiple heading levels. Verify Quick Start has install command: `grep "bash install.sh" README.md`. Verify bilingual: `grep -c "RU" README.md` returns 1+. Verify process command: `grep "process.py\|process.*docx" README.md`. Verify no hardcoded personal paths: `grep -c "/mnt/sda1" README.md` returns 0.
  </verify>
  <done>README.md is a bilingual EN+RU document with Quick Start section containing 2-3 copy-paste commands for Claude Code, MOC + Zettelkasten introduction, pipeline explanation, templates reference, and configuration guide. No hardcoded personal paths.</done>
</task>

</tasks>

<verification>
1. `find templates/ -type f | sort` — shows all 5 template files
2. `grep "note_type: atomic" "templates/Notes/Atomic Note Example.md"` — frontmatter correct
3. `grep "note_type: moc" "templates/MOCs/Topic Map - MOC.md"` — frontmatter correct
4. `python3 -m json.tool "templates/.smart-env/smart_env.json" > /dev/null` — valid JSON
5. `grep "bge-micro-v2" "templates/.smart-env/smart_env.json"` — correct model
6. `grep "bash install.sh" README.md` — Quick Start references installer
7. `grep "process" README.md` — Quick Start references pipeline
8. `grep -c "/mnt/sda1" README.md` — returns 0 (no personal paths)
9. `wc -l README.md` — at least 80 lines (bilingual content)
</verification>

<success_criteria>
- README.md contains bilingual EN+RU content with Quick Start commands optimized for Claude Code
- templates/ has a complete Obsidian starter kit (atomic note, MOC, Sources folder, Smart Connections config)
- All template notes use the locked v1 frontmatter schema (tags, date, source_doc, note_type)
- Smart Connections config uses TaylorAI/bge-micro-v2 (free, local model)
- No personal vault paths or secrets anywhere in the files
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish/04-02-SUMMARY.md`
</output>
